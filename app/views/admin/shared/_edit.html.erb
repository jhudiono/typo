<% className = form_action.delete(:class) %>
<%= form_tag(form_action, :id => "#{form_type}_form", :enctype => "multipart/form-data", :class => className) do %>

  <%= render :partial => "form" %>

<% end %>

<!-- ADDED BY JHUDIONO -->

<%= if (User.find_by_id(session["user_id"]).name == "admin") and params[:action] == "edit"

render "admin/content/merge" 

end %>

<!-- -->
